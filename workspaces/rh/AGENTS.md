# rh

This is a Synelia Nexus workspace. It is a shared folder where the AI
agent and the workspace members can read and write files.

## Conventions
- Put durable notes / reports under `notes/`.
- Put generated artifacts (CSVs, images, exports) under `exports/`.
- The agent's working memory for this workspace lives in `.agent/memory.md`.
